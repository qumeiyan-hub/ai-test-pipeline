[pytest]
testpaths = tests
python_classes = Test* *验证 *测试 *检查 *场景* *功能* *字段* *学员* *边界*
python_files = test_*.py *_test.py
addopts = 
    -v
    --html=reports/report.html
    --self-contained-html
    --tb=short
markers =
    smoke: 冒烟测试
    regression: 回归测试
    ui: UI测试
